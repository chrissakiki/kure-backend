info:
  name: Get Faqs
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/faqs"
  params:
    - name: page
      value: MAIN
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
