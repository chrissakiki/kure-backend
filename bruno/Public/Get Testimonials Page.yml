info:
  name: Get Testimonials Page
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/pages/testimonials"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
