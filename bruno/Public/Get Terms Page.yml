info:
  name: Get Terms Page
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/pages/terms"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
