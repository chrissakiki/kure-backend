info:
  name: Get Faq Page
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/pages/faq"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
