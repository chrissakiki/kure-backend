info:
  name: Get Privacy Page
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/pages/privacy"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
