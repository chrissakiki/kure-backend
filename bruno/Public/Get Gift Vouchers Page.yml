info:
  name: Get Gift Vouchers Page
  type: http
  seq: 7

http:
  method: GET
  url: "{{base_url}}/pages/gift-vouchers"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
