info:
  name: Get Packages Page
  type: http
  seq: 8

http:
  method: GET
  url: "{{base_url}}/pages/packages"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
