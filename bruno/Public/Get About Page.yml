info:
  name: Get About Page
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/pages/about"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
