info:
  name: Get Careers Page
  type: http
  seq: 9

http:
  method: GET
  url: "{{base_url}}/pages/careers"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
