info:
  name: Verify OTP
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/auth/verify-otp"
  body:
    type: json
    data: |-
      {
        "email": "akikichriss@gmail.com",
        "otp": "234030"
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: bru.setVar("token", res.body.data?.token);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
