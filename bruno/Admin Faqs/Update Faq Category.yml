info:
  name: Update Faq Category
  type: http
  seq: 9

http:
  method: PUT
  url: "{{base_url}}/admin/faqs/categories/{{category_id}}"
  body:
    type: json
    data: |-
      {
        "page": "MAIN",
        "label": "BOOKING & SCHEDULING",
        "title": "Booking your session.",
        "sortOrder": 1,
        "isActive": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
