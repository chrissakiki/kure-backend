info:
  name: Get Faq Category By Id
  type: http
  seq: 8

http:
  method: GET
  url: "{{base_url}}/admin/faqs/categories/{{category_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
