info:
  name: Get Faq Categories
  type: http
  seq: 6

http:
  method: GET
  url: "{{base_url}}/admin/faqs/categories"
  params:
    - name: page
      value: ""
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
