info:
  name: Get Faq By Id
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/admin/faqs/{{faq_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
