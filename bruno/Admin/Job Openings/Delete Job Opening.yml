info:
  name: Delete Job Opening
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{base_url}}/admin/job-openings/{{job_opening_id}}"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
