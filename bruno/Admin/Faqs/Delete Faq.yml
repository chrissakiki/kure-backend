info:
  name: Delete Faq
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{base_url}}/admin/faqs/{{faq_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
