info:
  name: Get Testimonial By Id
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/admin/testimonials/{{testimonial_id}}"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
