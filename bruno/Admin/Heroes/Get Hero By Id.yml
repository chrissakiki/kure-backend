info:
  name: Get Hero By Id
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/admin/heroes/{{hero_id}}"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
