info:
  name: Delete Hero
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{base_url}}/admin/heroes/{{hero_id}}"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
