info:
  name: Get Section Outro By Id
  type: http
  seq: 7

http:
  method: GET
  url: "{{base_url}}/admin/content/section-outros/{{section_outro_id}}"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
