info:
  name: Get Section Outros
  type: http
  seq: 6

http:
  method: GET
  url: "{{base_url}}/admin/content/section-outros"
  params:
    - name: page
      value: FAQ
      type: query
      disabled: true
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
