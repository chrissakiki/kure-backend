info:
  name: Get Offer Cards
  type: http
  seq: 9

http:
  method: GET
  url: "{{base_url}}/admin/content/offer-cards"
  params:
    - name: page
      value: PACKAGES
      type: query
      disabled: true
    - name: sectionKey
      value: PACKAGE_SIZES
      type: query
      disabled: true
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
