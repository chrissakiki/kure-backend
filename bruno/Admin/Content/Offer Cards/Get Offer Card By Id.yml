info:
  name: Get Offer Card By Id
  type: http
  seq: 11

http:
  method: GET
  url: "{{base_url}}/admin/content/offer-cards/{{offer_card_id}}"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
