info:
  name: Delete Milestone Stat
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{base_url}}/admin/content/milestone-stats/{{milestone_stat_id}}"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
