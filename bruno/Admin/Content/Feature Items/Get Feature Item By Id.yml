info:
  name: Get Feature Item By Id
  type: http
  seq: 16

http:
  method: GET
  url: "{{base_url}}/admin/content/feature-items/{{feature_item_id}}"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
