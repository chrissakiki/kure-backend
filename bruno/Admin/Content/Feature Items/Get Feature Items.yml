info:
  name: Get Feature Items
  type: http
  seq: 14

http:
  method: GET
  url: "{{base_url}}/admin/content/feature-items"
  params:
    - name: page
      value: PACKAGES
      type: query
      disabled: true
    - name: sectionKey
      value: PACKAGE_BENEFITS
      type: query
      disabled: true
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
