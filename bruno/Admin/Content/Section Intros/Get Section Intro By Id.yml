info:
  name: Get Section Intro By Id
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/admin/content/section-intros/{{section_intro_id}}"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
