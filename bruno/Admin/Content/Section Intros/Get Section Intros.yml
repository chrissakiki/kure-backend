info:
  name: Get Section Intros
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/admin/content/section-intros"
  params:
    - name: page
      value: PACKAGES
      type: query
      disabled: true
    - name: sectionKey
      value: PACKAGE_SIZES
      type: query
      disabled: true
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
