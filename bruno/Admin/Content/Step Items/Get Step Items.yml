info:
  name: Get Step Items
  type: http
  seq: 19

http:
  method: GET
  url: "{{base_url}}/admin/content/step-items"
  params:
    - name: page
      value: PACKAGES
      type: query
      disabled: true
    - name: sectionKey
      value: PACKAGES_HOW_IT_WORKS
      type: query
      disabled: true
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
