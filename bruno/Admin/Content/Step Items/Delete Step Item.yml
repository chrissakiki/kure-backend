info:
  name: Delete Step Item
  type: http
  seq: 23

http:
  method: DELETE
  url: "{{base_url}}/admin/content/step-items/{{step_item_id}}"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
