info:
  name: Get Step Item By Id
  type: http
  seq: 21

http:
  method: GET
  url: "{{base_url}}/admin/content/step-items/{{step_item_id}}"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
