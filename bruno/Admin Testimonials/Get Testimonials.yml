info:
  name: Get Testimonials
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/admin/testimonials"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
