info:
  name: Get Testimonial Category By Id
  type: http
  seq: 8

http:
  method: GET
  url: "{{base_url}}/admin/testimonials/categories/{{testimonial_category_id}}"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
